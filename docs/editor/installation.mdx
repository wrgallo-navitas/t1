---
title: Installation
description: Install LVGL Pro Editor on Windows, macOS, and Linux with platform-specific setup instructions and troubleshooting.
howTos:
  - name: "Install LVGL Pro Editor on Windows"
    description: "Complete step-by-step guide to installing LVGL Pro Editor on Windows, including WSL setup and editor installation."
    totalTime: "PT15M"
    estimatedCost: "$0 (free)"
    steps:
      - name: "Check if WSL is installed"
        text: "Open Windows Terminal or Command Prompt and run 'wsl.exe --list --verbose' to check if Windows Subsystem for Linux is already installed on your system."
        url: "#install-wsl"
      - name: "Install WSL if needed"
        text: "If WSL is not installed, run 'wsl --install' in Terminal and press Enter. Wait for the installation to complete. WSL is required for the editor to compile and run C code on Windows."
        url: "#install-wsl"
      - name: "Download the editor installer"
        text: "Visit pro.lvgl.io/#download in your web browser and click the Download button. Save the installer file to your Downloads folder or preferred location."
        url: "#install-the-editor"
      - name: "Run the installer"
        text: "Locate the downloaded installer file and double-click it to launch the installation wizard. Follow the on-screen prompts to complete the installation."
        url: "#install-the-editor"
      - name: "Launch LVGL Pro Editor"
        text: "After installation completes, find LVGL Pro Editor in your Start menu or desktop and launch it. The editor is now ready to use for creating LVGL user interfaces."
        url: "#install-the-editor"

  - name: "Install LVGL Pro Editor on macOS"
    description: "Step-by-step guide to installing LVGL Pro Editor on macOS with Podman setup for both Apple Silicon and Intel Macs."
    totalTime: "PT10M"
    estimatedCost: "$0 (free)"
    steps:
      - name: "Install Podman using Homebrew"
        text: "Open Terminal and run 'brew install podman' to install Podman. Podman is required for the editor to compile and run C code in containers. If you don't have Homebrew, visit brew.sh to install it first."
        url: "#install-podman"
      - name: "Download LVGL Pro Editor"
        text: "Visit pro.lvgl.io/#download and download the appropriate version for your Mac. Choose ARM for Apple Silicon Macs (M1/M2/M3/M4) or AMD for Intel-based Macs."
        url: "#install-the-editor"
      - name: "Install the application"
        text: "Open the downloaded file and drag the LVGL Pro Editor application to your Applications folder in Finder. This installs the editor on your system."
        url: "#install-the-editor"
      - name: "Launch the editor"
        text: "Open your Applications folder or use Spotlight search (Cmd+Space) to find and launch LVGL Pro Editor. The editor is now ready for creating LVGL user interfaces."
        url: "#install-the-editor"

  - name: "Install LVGL Pro Editor on Linux"
    description: "Complete guide to installing LVGL Pro Editor on Linux distributions using AppImage and Podman."
    totalTime: "PT10M"
    estimatedCost: "$0 (free)"
    steps:
      - name: "Install Podman for your distribution"
        text: "Install Podman using your package manager. For Debian/Ubuntu/Mint run 'sudo apt-get install podman'. For Fedora run 'sudo dnf -y install podman'. For Arch/Manjaro run 'sudo pacman -S podman'. Podman is required to compile and run C code."
        url: "#install-podman"
      - name: "Download the AppImage"
        text: "Visit pro.lvgl.io/#download and download the LVGL_Pro_Editor.AppImage file. Save it to a location like your Downloads or home directory."
        url: "#install-the-editor"
      - name: "Make the AppImage executable"
        text: "Open Terminal, navigate to where you saved the file, and run 'chmod +x LVGL_Pro_Editor.AppImage' to make it executable. This allows you to run the application."
        url: "#install-the-editor"
      - name: "Run the editor"
        text: "Launch the editor by running './LVGL_Pro_Editor.AppImage' in Terminal. If you encounter a GTK error, use './LVGL_Pro_Editor.AppImage --gtk-version=3' instead."
        url: "#install-the-editor"
---

Get LVGL Pro Editor running on your system with installation steps tailored to your operating system.

## Windows

### Install WSL

The Editor requires **Windows Subsystem for Linux** (WSL) to run on Windows.

First, check if WSL is already installed by opening a Terminal and running:

```bash title="bash"
wsl.exe --list --verbose
```

If WSL is not installed, run:

```bash
wsl --install
```

Then press Enter and wait for the installation to complete.

### Install the Editor

1. Go to [pro.lvgl.io/#download](https://pro.lvgl.io/#download)
2. Click the **Download** button
3. Save the installer to your computer
4. Run the installer and follow the prompts to complete the installation

## Linux

### Install Podman

The Editor compiles and runs C code in containers using **Podman** (similar to Docker). Install Podman for your Linux distribution:

- **Debian, Ubuntu, Linux Mint**: `sudo apt-get install podman`
- **Fedora**: `sudo dnf -y install podman`
- **Arch, Manjaro Linux**: `sudo pacman -S podman`

For other distributions, see the [Podman installation guide](https://podman.io/docs/installation#installing-on-linux).

### Install the Editor

1. Download the AppImage from [pro.lvgl.io/#download](https://pro.lvgl.io/#download)
2. Make it executable:
   ```bash
   chmod +x LVGL_Pro_Editor.AppImage
   ```
3. Run the application:
   ```bash
   ./LVGL_Pro_Editor.AppImage
   ```

### Troubleshooting

**Issue:** Double-clicking the `.AppImage` file doesn't launch the application, or you see a GTK error:

```bash
(process:3943): Gtk-ERROR **: 11:45:01.301: GTK 2/3 symbols detected.
Using GTK 2/3 and GTK 4 in the same process is not supported
Trace/breakpoint trap (core dumped)
```

**Solution:** Run the AppImage from the terminal with the GTK version specified:

```bash
./LVGL_Pro_Editor.AppImage --gtk-version=3
```

## MacOS

### Install Podman

The Editor compiles and runs C code in containers using **Podman** (similar to Docker). Install it using Homebrew:

```bash
brew install podman
```

For more information, see the [Podman installation guide for macOS](https://podman.io/docs/installation#macos).

### Install the Editor

1. Download the appropriate version from [pro.lvgl.io/#download](https://pro.lvgl.io/#download):
   - **ARM** for Apple Silicon (M1/M2/M3)
   - **AMD** for Intel-based Macs
2. Drag the application to your **Applications** folder in Finder
3. Launch the Editor from Applications or Spotlight